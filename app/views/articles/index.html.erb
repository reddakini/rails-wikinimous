<div class="container">
  <div class="row">
    <div class="col-sm-8 col-sm-offset-2">
      <% @articles.each do |a| %>
        <article>
          <h2> <%= link_to a.title, article_path(a) %> </h2>
          <%= a.content %>
        </article>
      <% end %>
    </div>
  </div>
</div>
